% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_kmeans.R
\name{sample_kmeans}
\alias{sample_kmeans}
\title{k-means sampling}
\usage{
sample_kmeans(
  input = NULL,
  clusters = 3,
  ncells = NULL,
  use_xy = FALSE,
  only_xy = FALSE,
  weights = NULL,
  layer_weights = NULL,
  xy_weight = NULL,
  candidates = NULL,
  candidate_weights = NULL,
  candidate_weight_col = NULL,
  min_cluster_size = 1,
  scale = TRUE,
  pca = FALSE,
  tol_pca = 0,
  n_pcs = NULL,
  num_init = 1,
  max_iters = 10,
  initializer = NULL,
  CENTROIDS = NULL,
  tol_kmeans = 1e-04,
  tol_opt = 0.3,
  seed = NULL,
  mini_batch = FALSE,
  batch_size = 10,
  init_frac = 1,
  early_stop = 10,
  filename_cl = NULL,
  args_cl = NULL,
  filename_d = NULL,
  args_d = NULL,
  sp_pts = FALSE,
  filename_pts = NULL,
  shp = FALSE,
  args_pts = NULL,
  cores = NULL,
  verbose = FALSE
)
}
\arguments{
\item{input}{An input \code{data.frame}, or a \code{SpatRaster} or \code{SpatVector}
object created by package \link[terra:terra]{terra}. The input should contain only numeric
variables for clustering.}

\item{clusters}{Number of clusters.}

\item{ncells}{Number of cells to extract, otherwise uses all cells.}

\item{use_xy}{Should the geographic xy coordinates be used as variables for
clustering?}

\item{only_xy}{Should the algorithm Use only xy coordinates?}

\item{weights}{A \code{SpatRaster} object or numeric vector with weights between
0 and 1.}

\item{layer_weights}{Numeric vector with weights for each input parameter.}

\item{xy_weight}{Numeric vector of weights for the x and y coordinates
(repeated if length 1).}

\item{candidates}{Candidate pool for selecting cluster centers. Supports:
\itemize{
\item numeric indices for rows/points in \code{input} (data frame and point-vector
inputs);
\item a \code{data.frame} when \code{input} is a \code{data.frame} (same column names and
classes; column order can differ);
\item a \code{SpatVector} with points when \code{input} is a \code{SpatVector} with points
(same attribute names/classes; candidate CRS is reprojected to match input if
needed);
\item a \code{SpatRaster} mask for spatial inputs.
}

When candidates map to rows/points in \code{input}, center selection is
constrained to those rows/points.}

\item{candidate_weights}{Optional numeric vector of positive weights for
candidate records. Supported when \code{candidates} is an independent
\code{data.frame} or \code{SpatVector} with points.}

\item{candidate_weight_col}{Optional name of a numeric, positive weight
column in candidate attributes. If both \code{candidate_weights} and
\code{candidate_weight_col} are provided, \code{candidate_weights} takes precedence.}

\item{min_cluster_size}{Minimum number of assigned rows/cells required to
retain a cluster. Clusters below this size are removed one at a time,
smallest first, and reassigned to the nearest surviving cluster, with
sizes refreshed after each removal.}

\item{scale}{Center and scale variables.}

\item{pca}{Use principal component analysis on variables.}

\item{tol_pca}{Tolerance for pca (remove PCs below threshold).}

\item{n_pcs}{Maximum number of principal components.}

\item{num_init}{See KMeans_rcpp.}

\item{max_iters}{See KMeans_rcpp.}

\item{initializer}{See KMeans_rcpp.}

\item{CENTROIDS}{KMeans_rcpp.}

\item{tol_kmeans}{See KMeans_rcpp.}

\item{tol_opt}{See KMeans_rcpp.}

\item{seed}{See KMeans_rcpp.}

\item{mini_batch}{Use MiniBatchKmeans (fast, less accurate).}

\item{batch_size}{See MiniBatchKmeans.}

\item{init_frac}{See MiniBatchKmeans.}

\item{early_stop}{See MiniBatchKmeans.}

\item{filename_cl}{File name for the output cluster raster.}

\item{args_cl}{List with arguments for writing the cluster raster.}

\item{filename_d}{File name for distance rasters.}

\item{args_d}{arguments for writing distance rasters.}

\item{sp_pts}{Output locations as spatial points.}

\item{filename_pts}{Filename for output locations.}

\item{shp}{Write output locations as a shapefile.}

\item{args_pts}{Arguments for writing output points.}

\item{cores}{Number of cpu cores to use.}

\item{verbose}{Print messages during processing.}
}
\value{
A list with components \code{clusters}, \code{distances}, and \code{points}.
}
\description{
Produces representative samples by clustering input data and selecting
one center per cluster from observed records. The function accepts tabular
data (\code{data.frame}), rasters (\code{SpatRaster}), and point vectors
(\code{SpatVector} with point geometry), and can include geographic coordinates
as clustering variables. Optional input weights affect sampling and distance
ranking, while optional candidate constraints restrict which rows/points can
be selected as final centers. Variables can be standardized, reweighted, and
reduced with PCA before clustering. Outputs include cluster assignments,
distances to assigned centroids, and selected center locations/indices.
}
